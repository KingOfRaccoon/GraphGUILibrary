package com.example.graphguilibrary.testModel

import android.content.Context
import android.graphics.Canvas
import android.graphics.Color
import android.graphics.Paint
import android.graphics.PointF
import com.example.graphguilibrary.Node

//class RedNode(center: PointF, radius: Float, context: Context, childNodeID: MutableList<Int>): Node(center, radius, context, childNodeID) {
//    override fun onDraw(canvas: Canvas) {
//        canvas.drawCircle(center.x, center.y, radius, Paint(Color.RED))
//    }
//
//}